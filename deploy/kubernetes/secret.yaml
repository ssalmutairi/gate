apiVersion: v1
kind: Secret
metadata:
  name: gate
  namespace: gate
  labels:
    app.kubernetes.io/name: gate
type: Opaque
stringData:
  # CHANGE THESE VALUES before deploying
  ADMIN_TOKEN: "changeme"
  DATABASE_URL: "postgres://gate:gate@postgres:5432/gate"
  # Uncomment if using Redis
  # REDIS_URL: "redis://redis:6379"
